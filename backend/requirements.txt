fastapi>=0.115
uvicorn[standard]>=0.30
SQLAlchemy>=2.0
pydantic>=2.7
pydantic-settings>=2.3
PyYAML>=6.0
python-dateutil>=2.9
httpx>=0.27

# --- Opcionales ---------------------------------------------------------------
# Conector de Noticias/RSS. Se importa de forma diferida; si no esta instalado,
# la ingesta de RSS se omite y el resto del monitor funciona igual.
#   pip install feedparser
# feedparser>=6.0
#
# Analisis con LLM (Claude). Sin esto se usa el analizador por reglas.
#   pip install anthropic
# anthropic>=0.40
